var name = "Bruce"; // variable set to Bruce
if (name != "Bruce") {   // if name var not set to Bruce it will ask what your name is but it is Bruce 
    console.log("What's your name?");
} else {    
    console.log("Hello, Bruce!"); // This is what will be printed
}